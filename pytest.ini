[pytest]
# The analyzer's modules (validator.py, loader.py, etc.) use flat imports
# ("from column_detector import ...") rather than package-relative ones,
# matching how main.py is actually run (`cd src && python main.py`) - see
# conftest.py, which adds src/ to sys.path so tests can import the same way.
testpaths = tests
